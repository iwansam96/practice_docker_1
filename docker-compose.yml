services:
  app-1:
    build: .
    ports:
      - "3001:3000"
  
  app-2:
    build: .
    ports:
      - "3002:3000"

  nginx:
    image: nginx:alpine-slim
    volumes:
      - type: bind
        source: nginx.default.conf
        target: /etc/nginx/conf.d/default.conf
        read_only: true
    ports:
      - "80:80"
    depends_on:
      - app-1
      - app-2
